#PBS -N {{ job_name }}
#PBS -l select=1:ncpus={{ ncpus }}:mem={{ mem }}:ompthreads={{ ncpus }}
#PBS -l walltime={{ walltime }}
#PBS -J {{ min_index }}-{{ max_index }}:{{ step }}
#PBS -e {{ job_log_dir }}
#PBS -o {{ job_log_dir }}

exec {{ cmd }}
